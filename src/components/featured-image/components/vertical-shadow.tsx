import styled from 'styled-components'

export const VerticalShadow = styled.div`
  width: inherit;
  height: inherit;
  background: linear-gradient(to top, rgb(235, 235, 235) 2%, transparent 50%, transparent 50% 100%);
`